[project]
name = "tensornetwork-benchmarks"
version = "0.1.0"
description = "Tensor network contraction benchmarks"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "fire>=0.5",
    "numpy>=1.20",
]

[tool.uv]
dev-dependencies = []

